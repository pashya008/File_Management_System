<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Page</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- <h1>Hello 
      <%= 2+2%>         it is use for calculation in ejs
    </h1> -->

    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md px-5 py-2 bg-zinc-800" type="text" placeholder="Enter Title" name="title" required />
                <textarea class="block w-full rounded-md px-5 py-2 mt-2 resize-none bg-zinc-800" placeholder="Write task" name="details" required></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 cursor-pointer" type="submit" value="Create Task" />
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10">
            <% if (files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <div class="flex w-full items-center justify-between mt-3">
                            <a class="text-blue-500 inline-block" href="/files/<%= val %>">Read More</a>
                            <a class="text-blue-500 inline-block" href="/edit/<%= val %>">Edit Filename</a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <h3 class="text-zinc-600">No Tasks Here</h3>
            <% } %>
        </div>
    </div>    
    <script src="/javascripts/script.js"></script>
</body>
</html>